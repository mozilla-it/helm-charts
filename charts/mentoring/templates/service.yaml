apiVersion: v1
kind: Service
metadata:
  name: {{ include "mentoring.fullname" . }}
  labels:
    {{- include "mentoring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mentoring.selectorLabels" . | nindent 4 }}
